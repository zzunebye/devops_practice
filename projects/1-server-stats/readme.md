# Server stats tool

A bash script to print out the server performance stats.

Project detail - https://roadmap.sh/projects/server-stats

## How to run

### Docker

```bash
docker build -t server-stats .

# --rm is for automatically removing the container after executing
docker run --rm server-stats
```